/** Built-in daily image deck (local-only cards; not synced to the server). */
export const PERMANENT_DECK_ID = "permanent";

/** Stable ids so schedules persist across sessions. */
export const PERMANENT_CARD_IDS = [
  "permanent-card-1",
  "permanent-card-2",
  "permanent-card-3",
  "permanent-card-4",
  "permanent-card-5",
] as const;
